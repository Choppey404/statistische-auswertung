{% extends 'base.html.twig' %}

{% block title %}Appointment{% endblock %}

{% block body %}
    <h1>Appointment</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ appointment.id }}</td>
            </tr>
            <tr>
                <th>FromDate</th>
                <td>{{ appointment.fromDate ? appointment.fromDate|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>ToDate</th>
                <td>{{ appointment.toDate ? appointment.toDate|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('appointment_list_index') }}">back to list</a>

    <a href="{{ path('appointment_list_edit', {'id': appointment.id}) }}">edit</a>

    {{ include('appointment_list/_delete_form.html.twig') }}
{% endblock %}
